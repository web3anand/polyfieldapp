{
  "name": "polyfield-app",
  "version": "1.0.0",
  "description": "PolyField - Polymarket Prediction Platform (Web + Mobile)",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev:all": "concurrently -n \"server,client\" -c \"blue,green\" \"npm run dev:server\" \"npm run dev\"",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "build:client": "npm run build --workspace=client",
    "build:server": "npm run build --workspace=server",
    "start:server": "npm start --workspace=server",
    "install:all": "npm install && cd client && npm install && cd ../server && npm install && cd ../shared && npm install && cd ..",
    "clean": "rm -rf node_modules client/node_modules server/node_modules shared/node_modules client/dist server/dist"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
