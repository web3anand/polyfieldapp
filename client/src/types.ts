/**
 * Re-export shared types for client use
 * Types are defined in ../../shared/types
 */

export type {
  Market,
  Position,
  Transaction,
  Trade,
  ClosedPosition,
} from '../../shared/types';
